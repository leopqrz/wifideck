# Runtime
fastapi>=0.115
uvicorn[standard]>=0.30      # includes websockets + httptools

# Dev / test
pytest>=8.0
httpx>=0.27                  # required by fastapi.testclient
ruff>=0.6                    # lint + format
